# Backlog production readiness

| ID | Модуль | Проблема | Источник | Приоритет | Влияние | Решение | Критерий приёмки | Статус | Тест | Браузер |
|---|---|---|---|---|---|---|---|---|---|---|
| SEC-001 | Production | Секреты и HTTPS имели небезопасные defaults | ТЗ 3.1 | P0 | Компрометация сессий и токенов | Fail-fast secrets, secure cookies, redirect, HSTS | Production не стартует без секретов; secure defaults включены | реализовано | settings/check | не требуется |
| SEC-002 | Файлы | Вложения открывались прямыми media URL | ТЗ 3.1, deny-by-default | P0 | IDOR и раскрытие ПДн | Авторизованные download endpoints с object ACL | Чужая организация/неучастник получает 403 | реализовано | journal/system security tests | ожидает DB |
| SEC-003 | Загрузки | IrpFile и TaskFile не ограничивали тип и размер | ТЗ 3.1 | P0 | Вредоносные/чрезмерные файлы | Общий серверный validator | php и файл >20 МБ отклоняются | реализовано | upload regression test | ожидает DB |
| RBAC-001 | Журнал/обмен | Роли ОП/СП не образуют явную матрицу действий | ТЗ 2.4, письмо §V | P0 | Избыточный доступ | Централизованная policy + transition matrix | Deny-by-default для каждого действия/роли | реализовано: capability policy и terminal-state FSM | permission/lifecycle tests | не требуется |
| EXC-001 | Обмен | Импорт не атомарен и не ограничен по размеру | ТЗ 2.2 | P0 | Частичные данные/DoS | atomic import, secure parser, quotas, уникальные имена | Ошибка не изменяет БД; лимиты проверены | реализовано | integration | не требуется |
| REQ-001 | Отчёты/обмен | Отсутствуют Приложения 1–10 | ТЗ 2.2, 2.5 | P0 | Нельзя доказать форматное соответствие | Получить утверждённые приложения | Формы и XSD сверены с оригиналом | заблокировано внешне | acceptance fixtures | нет |
| UX-001 | Журнал | Строки/сортировка недоступны с клавиатуры | WCAG 2.2, PRD v3 | P1 | Ключевой поток недоступен | Реальные ссылки, сохранение фильтров, aria-sort | Полный keyboard-only поток | реализовано | HTML/browser a11y | проверено: Enter открывает карточку |
| MSG-001 | Сообщения | Открытие диалога помечает все темы прочитанными | ТЗ 3.3 | P1 | Теряются непрочитанные | Отмечать только открытую тему | Неоткрытые темы сохраняют badge | реализовано | view regression test | нет |
| TASK-001 | Задачи | Ручной запуск и планировщик могли одновременно выполнить один job | ТЗ 3.6 | P1 | Дублирование импорта и побочных эффектов | Атомарный claim по статусу | Второй запуск пропускается без TaskRun | реализовано | model/view tests | не требуется |
| AUTH-001 | Аутентификация | Параллельные ошибки входа могли терять инкременты | ТЗ 3.1 | P1 | Блокировка наступает позже лимита | Условный атомарный UPDATE со счётчиком и блокировкой | Устаревшие экземпляры дают два инкремента; счётчик ограничен лимитом | реализовано | model/signal tests | не требуется |
| AUDIT-001 | Журнал событий | finished_at и duration отсутствовали у точечных/HTTP-событий | ТЗ 3.4 | P1 | Неполный обязательный аудит | Явный lifecycle point/pending | Каждое завершённое событие имеет start/end/duration | реализовано | model/middleware tests | не требуется |
| AUTH-002 | Сквозной вход | Были только JWT helpers без интеграционной точки и границы репозитория | ТЗ 3.1 | P1 | Внешняя подсистема не могла создать web-сессию | POST token exchange + EmployeeRepository | Валидный JWT создаёт сессию; invalid/inactive/locked отклоняются; open redirect исключён | реализовано | token/view tests | не требуется |
| AUTH-003 | Сквозной вход | Временный JWT можно было повторно обменивать на новые web-сессии до `exp` | Security audit | P0 | Перехваченный токен позволял replay | Обязательный `jti` и атомарная фиксация потребления в БД | Первый exchange успешен; повторный получает 403; гонка закрыта уникальным ключом | реализовано | token/view/full regression | не требуется |
| PREF-001 | Настройки таблицы | Сохранённый порядок колонок игнорировался реестром и терялся при повторном открытии формы | ТЗ 3.2 | P1 | Персональная раскладка фактически не применялась | Строить колонки и форму в сохранённом порядке | Порядок сохраняется между сеансами вместе с видимостью, сортировкой и фиксацией | реализовано | view tests | не требуется |
| TASK-002 | Задачи | После аварии worker задание навсегда оставалось running; планировщик не входил в поставку | ТЗ 3.6 | P1 | Автозагрузка прекращалась без ручной правки БД | Транзакционный stale recovery + compose scheduler | Старые run/event/task согласованно закрываются; свежий запуск не затрагивается | реализовано | command/model/compose checks | не требуется |
| JOURNAL-001 | Реестр | Не было печати отфильтрованного списка обращений | ТЗ 2.1 | P1 | Пользователь мог печатать только одну карточку | Отдельное print-view с текущими фильтрами, сортировкой и персональными колонками | Org scope сохраняется; вывод ограничен первыми 500 строками | реализовано | view/permission tests | ожидает browser print preview |
| USER-001 | Пользователи | UI скрывал фактические права и разрешал несовместимые с организацией либо произвольные группы | ТЗ 3.5 | P1 | Ошибочная выдача роли ОП сотруднику СМО и наоборот | Known-role queryset, org-role validation, матрица из server policy | Несовместимое назначение отклоняется; UI отражает фактические capabilities | реализовано | form/view tests | не требуется |
| UX-002 | Общие таблицы | Клиентская сортировка была mouse-only, неверно определяла числа и разрывала связанные строки задачи/события | PRD v3 2.0.3, WCAG 2.1.1 | P1 | Искажённый порядок и потеря контекста строки | Keyboard sorting + header data type + grouped rows | Enter/Space сортируют; aria-sort обновляется; detail/log остаётся рядом с владельцем | реализовано | JS syntax + template regression | проверено: Enter меняет порядок, task/log остаются парой |
| UX-003 | Пагинация | Ссылки дописывали новый `page` к уже существующему и могли оставить пользователя на старой странице | PRD v3, UX audit | P1 | Назад/вперёд открывали неверную страницу | Django querystring replacement во всех paginated templates | В URL ровно один page; активные фильтры сохраняются | реализовано | view regression | не требуется |
| DEPLOY-001 | Production | Image build запускал production settings без runtime-секретов; scheduler мог стартовать до миграций; PR не собирал образ | Production readiness | P0 | Невоспроизводимая поставка и гонка старта | Build-time base settings, health-gated scheduler, PR image build | Compose валиден; CI проверяет deploy settings, migrations и image build | реализовано | local checks; image build упёрся во внешний TLS EOF PyPI | не требуется |
| DOC-001 | Комплект поставки | Отсутствовали инструкция пользователя, описание БД, форматы обмена и runbook | ТЗ 6–7 | P1 | Нельзя безопасно установить, сопровождать и восстановить систему | Версионированные Markdown-документы в репозитории | Описаны эксплуатация/backup/restore, пользовательские потоки, схема и подтверждённые форматы | реализовано | review against code/requirements | не требуется |
| DOC-002 | Документация | Загрузка разрешала офисные документы, но отклоняла обязательные по ТЗ видеоролики | ТЗ 3.7 | P1 | Администратор не мог подключить обучающее видео | Отдельный video allowlist и лимит, защищённые просмотр и download | MP4/WebM/OGV до 200 МБ принимаются и просматриваются после входа; прочие и oversized отклоняются | реализовано | validator/view/ACL regression | browser пройден |
| PERF-001 | Производительность | Список сообщений и глобальный badge выполняли запросы на каждый диалог/сообщение; горячим фильтрам не хватало индексов | PRD v3, production readiness | P1 | Время ответа росло линейно с историей сообщений и объёмом журналов | Агрегаты/Subquery/bulk fetch и целевые составные индексы | Число запросов списка диалогов постоянно; миграции создают индексы под scope/date/scheduler | реализовано | query-count/full regression | не требуется |
| CONC-001 | Счётчики | Просмотры новостей и скачивания документов обновлялись из прочитанного значения | Production readiness | P2 | Параллельные запросы могли терять инкременты | SQL `F()` increment | Каждый запрос выполняет атомарный инкремент без read-modify-write | реализовано | view regression | не требуется |
| SEC-004 | Новости | Обложки не имели серверного ограничения размера | Production readiness | P1 | Чрезмерный upload расходовал память и хранилище | Model-level whitelist и лимит 10 МБ | Большая или неподдерживаемая обложка отклоняется до сохранения | реализовано | upload regression | не требуется |
| SEC-005 | Новости | Rich-text sanitizer принимал protocol-relative ссылки как локальные | OWASP URL validation | P1 | Администраторский контент мог скрытно увести пользователя на внешний домен | Структурная проверка scheme/netloc и single-slash local URL | `javascript:` и `//host` не попадают в href; HTTPS/mailto/local разрешены | реализовано | rendering regression | не требуется |
| UI-001 | Общая оболочка/реестр | Перегруженная двухстрочная навигация, равный визуальный вес действий и слабая иерархия основного экрана | UX-аудит | P1 | Медленнее считывается статус экрана и целевое действие | Однострочная scrollable navigation, компактный theme switch, page heading/metric, разделённый toolbar | Навигация не переносится; primary/secondary actions различимы; таблица явно доступна для горизонтальной прокрутки | реализовано | template/JS/browser QA | проверено desktop 1280px |
| UI-002 | Таблицы | Таблицы выглядели как плоские сетки, длинный УНр ломался на несколько строк, статусы заливали всю строку, действия расползались | UX-аудит | P1 | Сложно сканировать большие реестры и сопоставлять значения по строке | Общий dense table component, tabular numerals, zebra/hover, status rail, compact actions, explicit overflow | Одинаковая иерархия в журнале, событиях, задачах, пользователях, обмене и отчётах | реализовано | template/browser QA | проверено: журнал и события на 694px, задачи по AX |
| UI-003 | Карточка/форма обращения | Карточка была длинной однообразной лентой реквизитов, действия имели равный вес, а разделы большой формы слабо отделялись | UX-аудит | P1 | Оператор медленно находит нужный блок и рискует пропустить обязательное поле | Смысловая сетка карточки, нумерованные секции, единая панель действий, явные required-маркеры и фиксируемые действия формы | Статус и primary action видны сразу; реквизиты сканируются парами; мобильная форма остаётся одноколоночной | реализовано | template/browser QA | проверено на 694px: карточка и создание обращения |
| UI-004 | Сообщения | На мобильном пустой список занимал верх экрана, служебная подпись ссылалась на PRD, заголовки диалогов и тем собирались inline-стилями | UX-аудит | P1 | Основное действие и контекст обсуждения терялись, экран выглядел техническим | Рабочий heading, компактная панель диалогов, осмысленный welcome state, единые заголовки диалога/темы и mobile reordering | Создание и текущий контекст идут перед списком на узком экране; пустые состояния объясняют следующий шаг | реализовано | template/browser QA | проверено на 694px: список сообщений |
| UI-005 | Отчёты/задачи/новости/документы | Разделы начинались с технических описаний, CTA находились отдельно от заголовка, пустые состояния и карточки были несогласованы | UX-аудит | P1 | Портал выглядел набором служебных страниц без общей визуальной системы | Единые module headings, счётчики, основной CTA в заголовке, нейтральные scope/empty states и валидная структура карточек новостей | На всех четырёх разделах сразу читаются контекст и главное действие; карточки доступны ссылкой без вложенных интерактивных элементов | реализовано | template/browser QA | проверка узких экранов |
| UI-006 | Администрирование/обмен/настройки | Управляющие действия были смешаны с фильтрами, заголовки не объясняли контекст, отдельные кнопки содержали английские подписи | UX-аудит | P1 | Служебные экраны требовали повторного чтения и выглядели несогласованно | Единые page/module headings, CTA вне фильтров, русские подписи состояний, общие action groups и формы управления | Пользователи, события, обмен, настройки и формы имеют одинаковую иерархию и адаптивные действия | реализовано | template/browser QA | проверка узких экранов |
| UI-007 | Вход/обмен/маршрутизация | Вход выглядел как обычная служебная форма, загрузка и протокол использовали технические формулировки, переадресация смешивала primary и печатное действие | UX-аудит | P1 | Критические переходы не объясняли следующий шаг и визуально расходились с остальным порталом | Сфокусированный login, контекстные заголовки, file guidance, карточка результата ФЛК и единая иерархия действий | Гость не видит недоступную навигацию; загрузка объясняет ограничения; протокол и переадресация читаются как последовательный процесс | реализовано | template/browser QA | проверка узких экранов |
| UI-008 | Печать | Сопроводительное письмо печатало оболочку портала, preview и печатная версия использовали разные отступы, таблицы наследовали экранное sticky-поведение | UX-аудит | P1 | На бумагу попадали меню и служебные элементы; документ выглядел непредсказуемо | Общий A4 sheet/toolbar, print-only reset оболочки, статичные заголовки таблиц, адресный и подписной блоки | В печать попадает только документ; экранный preview похож на лист; карточка и реестр используют общие правила | реализовано | browser/print CSS QA | проверка preview |
| A11Y-001 | Формы | Labels и серверные/клиентские ошибки не везде были связаны с контролами через accessible description | UX-аудит, WCAG 1.3.1/3.3.1 | P1 | Screen reader не сообщал название и причину ошибки, визуальный label не всегда активировал поле | Общий DOM enhancement для `for`, `aria-invalid`, `aria-describedby` и live validation tip | Все стандартные `.field` получают связь label/control; ошибки и help text объявляются вместе с полем | реализовано | JS syntax + browser AX regression | форма обращения проверена по AX |
| UX-004 | Форма обращения | Поля жалобы и переадресации отображались независимо от выбранного сценария и могли сохранить устаревшие значения | Live UX-аудит | P1 | Длинная форма перегружена; в записи остаются неприменимые данные | Progressive disclosure по виду обращения/признаку направления и серверная нормализация | Условные поля появляются только в применимом сценарии; скрытые значения очищаются при сохранении | реализовано | form/view regression + JS syntax | проверено в live UI и AX tree |
| UX-005 | Форма обращения | Девять полей застрахованного всегда разворачивались, хотя обычно обращается сам заявитель | Live UX-аудит | P1 | Основной сценарий требует прокрутки нерелевантного блока | Доступный native disclosure без изменения модели или формата обмена | Пустой блок свёрнут; заполненный автоматически открыт; все поля доступны без JavaScript | реализовано | template/view regression | mobile UI и AX tree проверены |
| UI-009 | Реестр обращений | Групповая шапка заливала почти всю ширину зелёным, строки были высокими и полностью окрашивались по статусу | Замечание владельца, screenshot review | P1 | Таблица выглядела тяжёлой и хуже сканировалась | Локальные group-label chips, компактные ячейки, нейтральные строки и тонкий status rail | Зелёный фон ограничен подписью группы; статус не заливает строку; ключевые колонки читаются компактно | реализовано | template/full regression | 132 cases + desktop/mobile review |
| UI-010 | Реестр обращений | Полный UUID занимал непропорциональную ширину и выталкивал статус из видимой desktop-области | Продолжение screenshot review | P1 | Важный итоговый статус требовал горизонтальной прокрутки | Компактный хвост УНр в списке, полный идентификатор в accessible name и tooltip | Статус виден раньше; уникальная часть номера читается; полный УНр не теряется | реализовано | template/full regression | 132 cases + desktop/mobile review |
| DATA-001 | Обращения | Для пустого заявителя автоматически сохранялся паспорт РФ | Live form audit, XSD `z_doctype minOccurs=0` | P1 | Система создавала неуказанные пользователем персональные сведения | Удалить model default, сохранить nullable-контракт | Новое обращение без документа хранит `NULL`; пользователь явно выбирает тип при наличии документа | реализовано | model/form/migration regression | live form и AX tree проверены |
| FILE-001 | Документация/новости | Удаление записи оставляло файл документа или обложки в storage | Lifecycle audit | P1 | Недоступные файлы и старые данные бесконечно накапливались в backup/storage | `post_delete` cleanup через `transaction.on_commit` | Файл удаляется после успешного commit; откат БД не опережается удалением storage | реализовано | model/view transaction regression | не требуется |
| SEC-006 | Новости | Обложки выводились прямыми `MEDIA_URL`, обходя session ACL и не имея production endpoint | Media delivery audit | P0 | Скрытая новость могла раскрыть обложку через media-сервер; в production изображение могло не загрузиться | Авторизованный inline endpoint с проверкой статуса публикации | Гость перенаправлен на вход; обычный пользователь получает 404 для скрытой новости; администратор видит её | реализовано | endpoint/template/ACL regression | не требуется |
| FILE-002 | Журнал/сообщения/задачи | Каскадное удаление родительских записей оставляло файлы вложений в storage | Full file-model lifecycle audit | P1 | Вложения с данными граждан и сотрудников сохранялись без доступной записи-владельца | Общий commit-safe cleanup для `IrpFile`, `MessageAttachment`, `TaskFile` | Прямое и каскадное удаление очищает storage только после commit | реализовано | cascade transaction regression | не требуется |
| FILE-003 | Новости | Замена обложки сохраняла предыдущий файл без ссылки из БД | File replacement audit | P1 | Старые версии изображений бесконечно накапливались в storage и backup | Сравнение файла в `pre_save`, commit-safe удаление после `post_save` | Новая обложка доступна, предыдущая удалена только после успешного сохранения строки | реализовано | model transaction regression | не требуется |
| SEC-007 | Production/PostgreSQL | Compose допускал стандартный пароль `ejournal`, а production settings — пустой пароль | Deployment security audit | P0 | Компрометация основной БД при запуске с шаблонной конфигурацией | Fail-fast `DB_PASSWORD` в settings и Compose | Пустой, стандартный или короткий пароль останавливает запуск; CI использует отдельное безопасное значение | реализовано | settings subprocess + compose gate | не требуется |
| PERF-002 | Новости/документация | Поиск и подсказки загружали все записи и полный текст в Python; категории документации создавали N+1 count | Query audit | P1 | Память и время ответа росли со всем объёмом базы, а не с размером страницы | SQL `translate`/`strpos`, server-side limit, aggregate count | Поиск кириллицы выполняется в БД; подсказки ограничены SQL; счётчики категорий агрегируются одним запросом | реализовано | SQL capture + view regression | не требуется |
| SEC-008 | Portal frontend | Пять экранов исполняли inline JavaScript, Content Security Policy отсутствовала | Client security audit | P1 | Шаблонная инъекция могла выполнить script; строгий CSP нельзя было включить без поломки UX | Общий статический модуль и CSP `script-src 'self'` | В пользовательских шаблонах нет inline script; ответы содержат CSP; формы сохраняют поведение | реализовано | template/header/JS regression | browser matrix |
| UX-011 | Обмен | Клиентская подсказка ограничивала файл 10 МБ и допускала `.xls`, сервер — 20 МБ и только `.xlsx` | Form contract audit | P1 | Валидный файл 10–20 МБ блокировался браузером, а `.xls` отклонялся только после отправки | Единый лимит 20 МБ и одинаковый allowlist | Текст, accept, клиентская и серверная проверки совпадают | реализовано | template/JS/server regression | browser matrix |
| PERF-003 | Журнал/сообщения/задачи | Autocomplete загружал целые таблицы в Python; поиск диалогов зависел от locale-sensitive `icontains` | Endpoint/query audit | P1 | Рост памяти и задержки; кириллический поиск отличался между окружениями | SQL fold + server-side limit + active-user scope | Подсказки ограничены в SQL; org scope сохранён; неактивные сотрудники исключены | реализовано | SQL capture/scope regression | не требуется |
| UX-012 | Сообщения | Подсказка участника возвращала подпись, не совпадающую с checkbox label по порядку ФИО и организации | Live contract audit | P1 | Найденный сотрудник не отмечался в форме нового диалога | Структурированный `{id, label}` и выбор checkbox по primary key | Подсказка однозначно отмечает нужного активного сотрудника | реализовано | endpoint/form/JS regression | browser matrix |
| AUDIT-002 | Аутентификация | Middleware фиксировал пользователя до login view: успешный вход становился `login_failed`, SSO — анонимным `other`; неудача с HTTP 200 имела result `ok` | Event lifecycle trace | P0 | Журнал безопасности искажал факты входа и субъект действия | Раздельный actor до/после response и семантический result | Password/SSO success записаны как `login` с пользователем; отказ — `login_failed/failed`; logout сохраняет инициатора | реализовано | end-to-end middleware regression | не требуется |
| UI-013 | Реестр обращений | При ширине 1440 px последний столбец статуса частично уходил за правую границу таблицы | Live browser/screenshot audit | P1 | Итоговое состояние обращения не читалось без горизонтальной прокрутки | Sticky status column с фоном состояния и разделяющей тенью | Статус остаётся видимым справа при любом горизонтальном положении; hover/zebra/status backgrounds согласованы | реализовано | CSS/template regression | live desktop/mobile |
| UI-014 | Общие таблицы | Системный голубой header и зелёный hover конкурировали со статусами; подписи групп реестра оставались слишком заметными | Повторное замечание владельца, live screenshot audit | P1 | Цветовые заливки делали таблицы тяжёлыми и затрудняли быстрое считывание статуса | Нейтральные header/hover и почти белые контурные подписи групп | Цвет используется в badges и тонких смысловых маркерах, а не на ширине таблицы | реализовано | CSS regression/full suite | live narrow viewport |
| UI-015 | Реестр обращений | Даже контурные групповые chips воспринимались как цветная полноширинная шапка, а фон состояний оставался слишком заметным | Повторное замечание владельца, screenshot audit | P1 | Таблица выглядела декоративной и тяжёлой вместо рабочего реестра | Прозрачная групповая строка, короткая акцентная риска у подписи, менее контрастные header/zebra/state/hover | Зелёный акцент не заполняет группу; цвет строки не конкурирует со status badge | реализовано | CSS/template regression | live narrow viewport |
| A11Y-002 | Настройки интерфейса | Панель предлагала неверный ряд A−/A/A+ (15/17/19 px), не имела A++ и режимов «Белая/Чёрная» | PRD §5.2, v3 §2.12 | P1 | Обязательный режим слабовидящих не соответствовал размерам и контрасту из ТЗ | Обычный 17 px + A/A+/A++ 19/23/27 px, контраст default/white/black, сохранение localStorage | Режим контраста перекрывает тему; все кнопки имеют group label и aria-pressed | реализовано | template/JS/full regression | live prefs viewport |
| UI-016 | Глобальная навигация | При 1024–1366 px административное меню имело внутреннюю горизонтальную прокрутку | PRD v3 DoD, automated viewport audit | P1 | Часть разделов скрывалась за скроллом вопреки критерию приёмки | Сгруппировать users/events/admin в доступное меню «Управление» и проверять nav overflow автоматически | На 1024–1920 px меню в одну строку без scroll; активный подраздел обозначен | реализовано | template/full visual matrix | 165/165 cases passed |
| PERF-005 | Отчётность | Формы 1–8 загружали каждое обращение периода в Python для группировки и условных счётчиков | PRD v3 DoD, report query audit | P1 | Память и время формирования росли с числом обращений, а не с числом строк отчёта | SQL GROUP BY, filtered Count и Avg duration; theme label через JOIN | Каждая из девяти форм строится одним запросом без N+1; результаты регрессионных примеров неизменны | реализовано | query-count/report/full regression | не требуется |
| DATA-002 | Журнал/БД | Lifecycle-статус и хронология контролировались формой не полностью; ORM мог сохранить неизвестный статус или срок/закрытие раньше поступления | Persistence audit, PRD lifecycle | P0 | Повреждённая карточка искажала просрочку, отчёты и terminal-state guards | Model validation + DB CheckConstraints + нормализующая data migration | БД отклоняет неизвестный status и обратную хронологию независимо от пути записи | реализовано | model/constraint/migration/full regression | не требуется |
| SEC-011 | Пользователи/tenant scope | Код организации сотрудника ограничивался только choices формы; неизвестное значение делало область доступа неоднозначной и могло вызвать 500 | Tenant persistence audit, PRD org registry | P0 | Ошибочная изоляция данных или отказ служебных экранов | DB CheckConstraint по утверждённому ORGS; fail-closed data migration | Неизвестный org отклоняется БД; legacy-учётная запись предварительно деактивируется | реализовано | employee/core/migration/full regression | не требуется |
| FILE-004 | Автоматический обмен | Cross-filesystem fallback архивировал копию, но оставлял исходник; Excel-путь не имел fallback | File lifecycle root-cause audit | P0 | Scheduler повторно обрабатывал один входной файл на каждом запуске | Единый archive helper: replace либо EXDEV copy+unlink | После успешного архива источник отсутствует; повторный запуск не создаёт ImportLog; прочие OS errors не маскируются | реализовано | command/file lifecycle/full regression | не требуется |
| SEC-009 | Экспорт XLSX/PDF | Пользовательские строки записывались в Excel без нейтрализации формул, а PDF воспринимал их как ReportLab-разметку | Export path audit | P1 | Формула выполнялась при открытии файла; некорректная разметка могла сорвать PDF-экспорт | Общий sanitizer XLSX и XML escaping для PDF | Опасные префиксы остаются текстом; угловые скобки и ampersand безопасно попадают в PDF | реализовано | export regression/full suite | не требуется |
| UX-015 | Обмен | HTML file picker продолжал предлагать `.xls`, хотя JavaScript и сервер принимали только `.xlsx` | Contract re-audit | P1 | Пользователь мог выбрать заведомо неподдерживаемый файл | Синхронизировать `accept` с клиентской и серверной проверкой | Picker предлагает только XML/XLSX; `.xls` отсутствует во всех трёх слоях | реализовано | template/form regression | не требуется |
| PERF-004 | Журнал событий | Подсказка инициатора обходила весь справочник сотрудников в Python | Endpoint re-audit | P1 | Задержка и память росли со всеми учётными записями | SQL fold по ФИО/логину и server-side limit | Кириллица ищется в БД; запрос возвращает не более 10 строк | реализовано | SQL capture/query regression | не требуется |
| DEPLOY-002 | TLS/reverse proxy | Production требовал TLS-терминатор, но не учитывал `X-Forwarded-Proto`; Gunicorn публиковался на всех интерфейсах | Deployment topology audit | P0 | Циклический HTTPS redirect/CSRF failure либо обход proxy с подделкой scheme header | Явный proxy SSL contract и loopback bind Compose | HTTPS распознаётся за proxy; application port по умолчанию недоступен извне | реализовано | settings/compose/deploy checks | не требуется |
| SEC-010 | Рабочие вложения | Валидатор документации разрешил видео до 200 МБ также в обращениях, сообщениях и задачах; file picker задачи предлагал неподдерживаемый CSV | File-policy re-audit | P1 | Рабочие разделы могли принимать чрезмерные видео, а UI обещал ложный формат | Раздельные политики документации и рабочих вложений, единый allowlist UI/model/view | Видео до 200 МБ доступно только в документации; вложения ограничены документами до 20 МБ | реализовано | validator/form/view/full regression | не требуется |
| RBAC-002 | Редактирование обращения | СМО мог подменить первичного исполнителя или организацию через POST и перенести запись за границу своего tenant scope | Form authorization audit | P0 | Несанкционированное изменение принадлежности и потеря записи из области организации | Неизменяемый первичный владелец и server-side scoped choices для организаций/исполнителей | Поддельный POST не меняет владельца; чужие org/employee отклоняются формой | реализовано | form/view tenant regression | не требуется |
| MSG-002 | Сообщения | Закрытая тема скрывала форму ответа, но endpoint продолжал принимать прямой POST | Mutation endpoint audit | P0 | Участник мог добавлять сообщения и вложения после закрытия обсуждения | Server-side terminal-state guard до создания reply | POST в закрытую тему возвращает 403 и не создаёт сообщение, файл или SEND audit | реализовано | endpoint/side-effect regression | не требуется |
| REP-001 | Отчётность | Обязательный период проверялся только JavaScript, direct GET формировал отчёт без него; общая ошибка формы не отображалась | PRD v3 §2.5.5, report path audit | P1 | Неограниченная выборка нагружала web-процесс, пользователь не видел причину отказа | Server-side period validation и вывод non-field errors | Preview без границы периода не строится; прямой export получает 400; ошибка видима в форме | реализовано | form/view/export regression | не требуется |
| TASK-003 | Задачи | Администратор мог вручную поставить статус `running` без запуска и навсегда исключить задачу из scheduler/stale recovery; ручной режим сохранял скрытый интервал | Task lifecycle audit, PRD v3 §2.11 | P0 | Задание зависало без `last_started_at`, расписание становилось недостоверным | System-owned status, active assignee scope, server normalization режима | POST не подменяет статус; manual очищает интервал; неактивный исполнитель не назначается | реализовано | form/view/scheduler regression | не требуется |
| TASK-004 | Задачи | Поле команды оставалось свободным text input: присваивание `.choices` полю `CharField` не включало select и валидацию | Live form audit, PRD v3 §2.11 | P1 | Можно было создать заведомо неисполняемую задачу вне реестра команд | Настоящий `ChoiceField` из `TASK_COMMAND_CHOICES` | UI показывает понятный список; неизвестная команда отклоняется до сохранения | реализовано | form/view/live regression | live mobile |
| TASK-005 | Задачи/БД | Инварианты команды, статуса и расписания действовали только в форме; старые/ORM-записи могли остаться неисполнимыми | Persistence audit | P0 | Scheduler мог навсегда пропускать повреждённую задачу или выполнять неизвестную команду | Нормализующая data migration и DB CheckConstraints | Некорректные legacy-строки безопасно отключены; БД отклоняет новые нарушения | реализовано | migration/constraint/full regression | не требуется |
| DATA-003 | Обмен/БД | Организация, вид файла и статус протокола ограничивались только Django choices | Persistence audit | P0 | ORM/SQL мог создать неоднозначный или невидимый журнал импорта | Нормализующая data migration и DB CheckConstraints | Legacy-строки безопасно классифицированы; неизвестные `org`, `kind`, `status` отклоняются БД | реализовано | migration/constraint/full regression | не требуется |
| DATA-004 | События/БД | Тип, результат и lifecycle события аудита не были закреплены базой данных | Persistence audit | P0 | Прямая запись могла исказить категорию, результат или временную линию обязательного аудита | Нормализующая data migration и DB CheckConstraints | Закрытые справочники, непустой модуль, парное завершение и монотонное время проверяются БД | реализовано | constraint/full regression | не требуется |
| SEC-012 | Portal frontend | CSP продолжала разрешать inline CSS после удаления всех style-атрибутов из пользовательских шаблонов | CSP re-audit | P1 | HTML-инъекция могла менять визуальное представление и скрывать элементы интерфейса | Строгий `style-src 'self'` и template hygiene ratchet | В CSP нет `unsafe-inline`; шаблоны без style/event attributes | реализовано | header/template/full regression | не требуется |
| DEPLOY-003 | Docker/CI | Базовые контейнеры и GitHub Actions подключались по плавающим тегам | Supply-chain audit | P0 | Повторная сборка могла незаметно выполнить изменившийся сторонний код | Multi-arch image digest и полные commit SHA Actions | Dockerfile и workflow не используют неприкреплённые внешние executable refs | реализовано | static regression + Docker build | не требуется |
| SEC-013 | Python dependencies | Frozen lockfile не проверялся на опубликованные уязвимости | Supply-chain audit | P0 | Известная CVE могла попасть в production image при зелёных функциональных тестах | Обязательный PyPA pip-audit по `.venv` с OSV backend | PR останавливается при найденной уязвимости; action закреплён commit SHA | реализовано | live OSV query + workflow ratchet | не требуется |
| DEPLOY-004 | Dependency sync | `uv sync` собирал ненужный editable wheel приложения через внешний Hatchling, тогда как Docker запускал исходники напрямую | Reproducibility audit | P1 | Чистая установка зависела от лишнего обращения к package index и расходилась с image build | Virtual uv project (`package = false`) | Frozen sync устанавливает только lockfile и не собирает локальный wheel | реализовано | offline/frozen sync + full gate | не требуется |
| DEPLOY-005 | Container runtime | Web и scheduler запускались от root; Compose PostgreSQL оставался на плавающем теге | Container security audit, PRD infrastructure | P0 | Компрометация процесса давала лишние права внутри контейнера; DB image мог измениться без review | UID/GID 10001, writable volume ownership, PostgreSQL digest | Image user не root; media/exchange доступны приложению; все production images immutable | реализовано | static/runtime image + compose gate | не требуется |
| UI-017 | Реестр обращений | На узком экране wide-table обрезала идентификаторы, а sticky-статус перекрывал содержимое; групповая строка воспринималась как цветная полоса | Повторное замечание владельца, live/automated screenshot audit | P1 | Ключевые реквизиты нельзя было сопоставить без горизонтальной прокрутки | Карточная компоновка до 720 px, labels из метаданных колонок, локальные нейтральные group labels, отключение sticky-status на tablet | На 694 px все значения помещаются без document overflow; на 1024–1920 px таблица сохраняет семантику и не имеет полноширинной зелёной заливки | реализовано | template/CSS/full visual matrix | 165/165 cases passed |
| DEPLOY-006 | Health/readiness | Compose принимал redirect `/healthz` как healthy и не проверял соединение web с PostgreSQL | Runtime availability audit | P0 | Gunicorn с потерянной БД считался здоровым; scheduler запускался при неготовом приложении | Раздельные liveness `/healthz` и DB-backed readiness `/readyz`; только точный HTTP 200 | Readiness выполняет `SELECT 1`, при DatabaseError отдаёт 503 без деталей; probe проходит HTTPS redirect | реализовано | endpoint/failure/compose regression | runtime compose contract |
| DEPLOY-007 | Service lifecycle | Основные сервисы не перезапускались после host reboot/crash; shell оставался PID 1 у web, scheduler работал через бесконечный shell-loop | Runtime lifecycle audit | P0 | SIGTERM мог не дойти до Gunicorn/Python; задача обрывалась, сервис не восстанавливался автоматически | restart policy, init, exec Gunicorn, signal-aware Django scheduler daemon | db/web/scheduler восстанавливаются unless explicitly stopped; web и scheduler получают SIGTERM и 75 секунд grace | реализовано | command/compose/static/full regression | container lifecycle contract |
| CI-001 | CI supply chain | Service container PostgreSQL оставался на плавающем теге; workflow наследовал неявные token permissions и не имел timeout/concurrency guard | CI security audit | P0 | Тестовая среда могла измениться без review; зависший или вытесненный run расходовал runner, checkout сохранял credential | Digest pin, read-only permissions, no persisted checkout credential, bounded/cancellable jobs | Все executable refs immutable; GITHUB_TOKEN только contents:read; обе job ограничены 30 минутами | реализовано | workflow static ratchet + full CI-equivalent gate | GitHub Actions run |
| DB-001 | PostgreSQL connections | `psycopg-pool` входил в обязательный стек, но production использовал одиночные persistent connections без health check | Runtime/database audit, PRD stack | P0 | После restart/failover worker мог повторно использовать мёртвое соединение; ожидание подключения не ограничивалось | Native Django psycopg pool, health checks, bounded connect/pool timeouts and sizes | Production возвращает соединение в pool после запроса, проверяет его перед выдачей; неверные лимиты fail-fast | реализовано | settings subprocess/config/full regression | runtime DB failover |
| SEC-014 | Production environment | Ошибка в булевой security-переменной молча отключала настройку; wildcard/URL принимались в `ALLOWED_HOSTS`, HSTS не имел диапазона | Configuration fail-safe audit | P0 | Опечатка могла отключить secure cookie/redirect/proxy contract или разрешить произвольный Host header | Strict boolean parser, explicit host allowlist, bounded HSTS | Неоднозначное значение, пустой/wildcard/malformed host и HSTS вне диапазона останавливают startup | реализовано | isolated production settings regression | не требуется |
| OPS-001 | Container logs | Docker host использует `json-file`, но Compose не ограничивал размер логов постоянных сервисов | Disk exhaustion audit | P1 | Web/scheduler/PostgreSQL могли занять весь системный диск и остановить БД и приложение | Общий logging anchor: 10 МБ × 5 файлов, compression | Каждый Compose-сервис наследует ограниченную ротацию; суммарный несжатый предел около 200 МБ | реализовано | compose/static/full regression | host capacity monitoring |
| SEC-015 | Token login | CSRF-exempt token exchange принимал browser POST с произвольного origin | Auth endpoint re-audit, Django/OWASP CSRF guidance | P0 | Login-CSRF мог принудительно заменить сессию сотрудника; cross-site запрос мог преждевременно погасить одноразовый jti | Fetch Metadata + Origin/Referer allowlist before token decode/consume | Same-origin и server-to-server разрешены; cross-site отклонён до consume; SSO origin только explicit HTTPS allowlist | реализовано | browser metadata/origin/replay/settings regression | integration origin contract |
| SEC-016 | Dynamic HTTP responses | Только login и token exchange явно запрещали кеширование; redirects, errors, карточки, отчёты и downloads полагались на defaults | Live response-header audit, personal data scope | P0 | Browser/shared proxy мог сохранить ПДн после logout или выдать ответ другой сессии | Central dynamic response cache middleware; static exclusion | Любой dynamic status/content type получает no-store/no-cache/private; static сохраняет WhiteNoise cache policy | реализовано | authenticated/redirect/error/header/full regression | reverse proxy must preserve headers |
| SEC-017 | Authentication routes | `django.contrib.auth.urls` неявно публиковал reset/change/confirm endpoints без продуктового UI и почтового operational contract | Auth surface audit, closed corporate resource | P1 | Чужие admin templates, скрытая неподдерживаемая функция и SMTP failure path расширяли attack/incident surface | Explicit LoginView/LogoutView plus existing token endpoint only | Нужные route names сохранены; все неутверждённые standard auth endpoints возвращают 404 | реализовано | route regression/full flow suite | password operations remain admin-controlled |
| SEC-018 | Active sessions | Блокировка сотрудника применялась только при новой аутентификации; существующая session могла восстановиться после разблокировки | Session lifecycle audit | P0 | Заблокированная учётная запись сохраняла выданный ранее session credential | Backend state check + irreversible cleanup of rejected session | Следующий запрос становится анонимным, отправляется на login и удаляет auth key из session | реализовано | backend/session/full regression | не требуется |
| SEC-019 | Session lifetime | Production наследовал стандартный срок Django в 14 дней и persistent browser cookie | Session lifecycle audit, personal data scope | P0 | Оставленная рабочая станция или сохранённая cookie слишком долго сохраняла доступ к ПДн | Bounded sliding idle timeout + browser-close cookie | Default 8 часов; допустимый диапазон 5 минут–12 часов; активность продлевает session | реализовано | isolated production settings/full regression | reverse proxy must not cache responses |
